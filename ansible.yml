---
- hosts: node
  become: true
  tasks:
    - name: create new deployment
      command: kubelet apply -f /home/ubuntu/Deployment.yml
    - name: create new service
      command: kubelet apply -f /home/ubuntu/Service.yml 
